function sumArray(numbers) {
    return numbers.reduce((sum, num) => sum + num, 0);
    //reduce is like gekuha nya ang array nya iya ireduce to a single value nya iadd niya
}

// Example usage:
console.log(sumArray([1, 2, 3, 4, 5])); 
